# Copy to .env for local development.
# Vite inlines VITE_* values at build time. Set these in Vercel/Render before building.

# Backend origin. Leave empty to use same-origin / Vite proxy.
VITE_API_URL=http://localhost:5000

# Optional local Vite port
VITE_DEV_PORT=5173
